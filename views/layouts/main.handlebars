<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Registry</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/jass.css">
</head>

<body>
  <nav>
    <button class="btn" id='home'><a href="/">Home</a></button>
    <button class="btn" id='home'>Dashboard</button>

    <!-- Only display the logout button if the user is logged in -->
    {{#if logged_in}}

    <button class="btn" id="logout">logout</button>
    {{else}}
    <button class="btn" id='home'><a href="/login">Login</a></button>
    {{/if}}
    <ul class="nav justify-content-end">
      <li class="nav-item">
        <button class="btn" id='newPost'><a class="nav-link active" href="/post">New Post +</a></button>
      </li>
    </ul>
    <nav>
      <h1 class="tech-header">THE TECH BLOG</h1>
    </nav>
    <ul class="nav justify-content-end">
  </nav>
  </header>
  <main class="container container-fluid mt-5">
    {{{ body }}}
  </main>
  <footer>

  </footer>

  <!-- Only include the logout script if the user is logged in -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>